
project.ext {
  riak = "1.0.5"
}

dependencies {
  runtime "org.slf4j:jcl-over-slf4j:${ext.slf4j}"
  
  compile ("com.basho.riak:riak-client:${ext.riak}") {
    exclude group: "com.google.protobuf", module: "protobuf-java"
    exclude group: "commons-codec", module: "commons-codec"
  }
}